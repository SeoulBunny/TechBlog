
const ContainerLayouts = ({children}:{children:React.ReactNode}) => {
  return (
    <section className="w-[90%] xl:w-[75%] mx-auto mt-30">
        {children}
    </section>
  )
}

export default ContainerLayouts